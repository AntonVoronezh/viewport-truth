## Versioning & releases

Releases are automated via semantic-release.

- Use `fix:` for patch releases
- Use `feat:` for minor releases
- Use `feat!:` (or `BREAKING CHANGE:` in the body) for major releases
